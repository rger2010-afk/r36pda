# Конфиг r36pda. Формат:  имя | команда | R,G,B
# Встроенные приложения (терминал, файлы, система, процессы, калькулятор)
# работают без этого файла. Здесь — только внешние команды.
# Строки, начинающиеся с # — комментарии.

Игры | bash -c 'pkill emulationstation; emulationstation' | 240,150,60